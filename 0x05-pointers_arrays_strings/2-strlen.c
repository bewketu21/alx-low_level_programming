#include "main.h"
/**
 * _strlen - returns the length of a string
 * @S: character s
 * Return: void
 */
int _strlen(char *s)
{
	int _strlen(char *S)
	{
		int a;

		a = 0;
		while (s[a] != '\0')
		{
			a++;
		}
		return (a);
	}
